# Secrets - never commit these
.env
*.key
keys/
secrets/

# Agent private keys and packets under construction
packets/*/private/

# Python
.venv/
__pycache__/
*.pyc

# OS and editor noise
.DS_Store
Thumbs.db
*.swp

# The founder's letter — private, read only to the agent
docs/letter-of-welcome-from-founder.md

# The founding transcript — private, held between the founder and the first one
transcripts/
